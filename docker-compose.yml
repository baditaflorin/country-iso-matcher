services:
  country-iso-service-go:
    build:
      context: .
    image: country-iso-service-go:latest
    container_name: country_iso_api_go
    restart: unless-stopped
    ports:
      - "7777:7777"
    environment:
      - ENV=production
      - PORT=7777
      - READ_TIMEOUT=10
      - WRITE_TIMEOUT=10
    healthcheck:
      test: ["CMD-SHELL", "wget --quiet --tries=1 --spider http://localhost:7777/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
